# product/CHANGELOG.md
# 门窗诊断系统 · 产品决策历史
> 记录所有重大产品决策、架构发现、验收结论。
> 技术实现变更记录在代码仓库，此处只记录决策层事件。

---

## [v3.9.7] · 2026-04-06 · 当前版本（已修复Bug 1和Bug 2）

### 决策事件：双档并列逻辑修复为原始规格（P0）

**触发**：createTender 和 generateReport 双档逻辑不一致，B档触发条件错误。

**修改文件**：
- `createTender/documentMapper.js`：收窄 shouldShowDualTier() 触发条件
- `generateReport/documentMapper.js`：重写双档逻辑，统一为 A 档触发
- `pdfBuilder-v2.js`：修复渲染层与数据结构不匹配问题（P0）
- `budgetSpec.js`（两份同步）：修正差价基准描述（P1-A）

**核心变更**：
- 触发条件：仅 A 档 + (楼层>16 OR 高度比>0.6 OR 交通噪声) 时触发双档
- 显示内容：A档（current）+ B档（recommended）双档并列
- B/C/D 档不触发双档显示

**技术实现**：
- `TRAFFIC_NOISE_TYPES`：统一为 ['rail', 'highway', 'metro', 'airport']
- `shouldShowDualTier()`：两份文件完全一致
- `buildUpgradeReasons()`：生成升级原因文案（无建议/应当等指令型语言）
- `calcCostDelta()`：计算档位间差价（基于中位值）
- PDF渲染：读取 c3.recommendedConfig 数组，current/recommended 双档堆叠布局
- mapToSections()：修复数据传递，保留 is_dual_tier 和 recommendedConfig 数组结构

**数据结构**：
```javascript
{
  is_dual_tier: true,
  recommendedConfig: [
    { displayRole: 'current', tier: 'A', spec: {...} },
    { displayRole: 'recommended', tier: 'B', spec: {...}, upgradeReasons: [...], costDelta: {...} }
  ]
}
```

**文案修正**：
- `BUDGET_TIER_GLASS_BASE.A.config`：从"双白玻中空（5+12A+5）"修正为"夹胶中空"
- `calcCostDelta.note`：从"相对于 A 档标准配置：双白玻中空..."修正为"相对于本案A档实际配置（夹胶中空）"

**测试覆盖**：139/139 passing（原有 134 + 新增 DM-42~46）

### Bug修复（生产PDF渲染）

**Bug 1（P0）：型材/五金/密封显示 [object Object]**
- 根因：BUDGET_SPEC中profile/hardware/seal是对象类型
- 修复：pdfBuilder-v2.js新增formatSpecField()函数
- 处理对象字段：min_wall_thickness→"壁厚≥Xmm"、min_load_kg→"铰链负载≥Xkg"、layers→"X道密封(X)"

**Bug 2（P1）：A档玻璃仍显示「双白玻中空（5+12A+5）」**
- 根因：A档玻璃fallback值在pdfBuilder-v2.js中错误
- 修复：A档玻璃fallback修正为"夹胶中空玻璃"（与B档一致）

**异常1说明**：冷启动需微信开发者工具实际测量
- 根因：之前0.006秒是本地Node.js调用，不含云函数容器启动
- 真实云函数冷启动：需微信开发者工具实际测量
- 预期冷启动：3-10秒（含容器启动）

**异常2修复**：非触发场景档位矛盾
- 根因：mapToSections使用...budgetSpec展开运算符，spec结构混乱
- 修复：重构chapter3数据结构，明确spec字段定义
- 验证：139/139 passing，A档非触发场景正确显示"经济实用 A档"

---

**异常1：冷启动时间核查**
- 根因：之前测量的0.006秒是本地Node.js调用，不包含云函数容器启动
- 真实云函数冷启动：需通过微信开发者工具或生产端点实际测量
- 建议：间隔5分钟触发冷启动，对比冷/热启动响应时间

**异常2：非触发场景档位矛盾** ✅ 已修复
- 根因：mapToSections使用`...budgetSpec`展开运算符，在非双档路径导致spec结构混乱
- 修复：重构chapter3数据结构，明确spec字段定义
- 验证：A档非触发场景正确显示"经济实用 A档"

---

## [v3.9.8] · 2026-04-05

### 决策事件：B档双档并列显示（P1）

**触发**：B档价格跨度较大（900–1400元/㎡），用户需要在基础配置和增强配置之间选择。

**修改文件**：`documentMapper.js` + `pdfBuilder-v2.js`
- `buildBudgetSpecView()`：为B档生成双档数据结构（base_config + enhanced_config）
- PDF渲染：B档显示"基础配置B档"和"增强配置C档"两个配置表
- 其他档位：保持单档显示逻辑不变

**数据结构**：
- `is_dual_tier`：标记是否双档显示
- `base_config`：B档标准配置
- `enhanced_config`：C档配置作为增强选项

**测试基线**：134/134（新增B档双档功能，原有测试基线保持）

---

## [v3.9.6] · 2026-04-05

### 决策事件：PDF内容标注修正（P1）

**触发**：生产文件审核发现标准依据、玻璃配置说明缺失，需补充专业标注。

**修改文件**：`documentMapper.js`（两份同步）+ `budgetSpec.js`（两份同步）
- SPEC-01：隔声参数来源标注更新为「GB 50118 + HJ 2055 · 轨道交通中距离声学计算推导值」
- SPEC-02：A档玻璃配置补充工程余量说明（建议第三方Rw检测报告）
- SPEC-03：B档隔音升级玻璃规格优化为 1.52PVB（稳定保障Rw≥40dB）
- SPEC-04：风压等级补充标注「对应GB/T 7106抗风压等级P4及以上」

**测试基线**：134/134（无计算逻辑变更，基线不变）

---

## [v3.9.5] · 2026-04-05

### 决策事件：文案层noise_type感知修复（P1）

**触发**：PDF生产文件审核发现第3章升级建议文案静态写死，交通噪声用户看到「需三玻两腔」与仲裁主路径结论矛盾。

**修改文件**：`documentMapper.js`（两份同步）
- 隔音升级描述：交通噪声场景替换为夹胶中空升规格建议
- glassDirection：交通噪声场景移除三玻两腔正向推荐
- fallback数组（index.js / pdfBuilder-v2.js）：不修改，正常路径不触发

**新增测试**：DM-40
**测试基线**：134/134

---

## [v3.9.4] · 2026-04-05

### 决策事件：Rw边界修复 · P0技术债关闭

**触发**：calculator-v2.js 非对称中空路径高估（34–38dB段），业主可能拿到含错误隔音参数的招标文件。

**核查过程**：
- 发起五任务外部核查，来源：ift Rosenheim实测报告 + Guardian GlassTime手册 + ISO 10140/717-1标准文件
- 关键发现：非对称中空因250–500Hz吻合效应，实测Rw工程上限约32–35dB；夹胶中空起始点36–38dB
- 切换点决策：取35dB（保守原则，避免承包商以非对称中空勉强应付35dB需求）

**决议**：
- `rw_max` 封顶：非对称中空路径从36dB收回至34dB
- 切换点：Rw≥35dB切换至夹胶中空路径（`laminated_hollow` level 4）
- 三玻两腔限制：交通噪声场景从eligibleLevels排除，原因是Rw可达但Ctr差
- 防御性 `is_risk` 标记保留，未来数据异常时兜底

**修改文件**：`shared/budgetSpec.js` / `arbitrator.js` / `documentMapper.js`（补传noise_type）

**测试**：133/133 passing（含v3.9.4新增8条边界回归 AR05–AR12，超出原基线125/125） → v3.9.5 升级至 134/134

**原则确立**：引入ift/Guardian/ISO外部实测数据作为边界标定依据；国内GB/T 8485与ISO 10140/717-1体系一致，数据直接适用中国市场。

**开放项**：
- [x] 125/125 passing → 133/133 通过（含AR05–AR12）
- [x] noise_type空值默认行为 → 补加显式默认值 `noise_type ?? 'quiet'`
      + AR12覆盖，两份arbitrator.js同步修改

---

## [v3.9.3] · sprint8/a-8a

### 已完成
- B-13 供应商报价录入模块上线（商家侧）
- V-01~V-06 视觉修复完成（9页PDF）
  - V-01/02：深蓝封面背景恢复
  - V-03/04：预算可视化条形图（从纯文本回归图形）
  - V-05/06：红线视觉样式优化
- 生产部署：generateReport + createTender 上线
- 测试：125/125 passing

### 已知问题（待解决）
- 🔴 Rw 34–38dB 段边界高估（calculator-v2.js）

---

## 架构冲突识别 · sprint8 前期

### 决策事件：三方文档比对分析
**触发**：将现有代码与两份补充技术文档进行三方对比
**发现**：
1. 双玻璃推荐系统并存（预算驱动 + 性能驱动），无仲裁机制
2. `calcRw` 跨重叠变量存在双重计数
3. `size_level` 字段语义混用（不同窗型共用同一字段）

**决议**：
- 引入 `resolveGlassConfig()` 统一仲裁函数
- 重构 `calcRwRequired()`，明确变量边界
- Q8 合并窗型尺寸维度，保持问卷9题总量不变

**原则确立**：竞争推荐系统必须有显式仲裁逻辑，否则输出不可预测。

---

## 价格档位复盘 · sprint8 前期

### 决策事件：价格区间重新标定
**旧档位**：[未记录]
**新档位**：
- A：600–900元/㎡
- B：900–1400元/㎡
- C：1400–2000元/㎡
- D：2000+元/㎡

**开放项**：
- A档900上限：需承包商价格实地确认
- B档跨度较大：PDF中需呈现双行配置（基础 + 增强）

**冲突提示语言决策**：
- ❌ 指令型（"建议升级至C档"）
- ✅ 信息呈现型（"若选择C档，预计增加成本△X元，可获得△Y性能提升"）

---

## 内容真实性验证 · sprint7 后期

### 决策事件：计算引擎交叉验证
**方法**：从PDF输出反向还原问卷输入，手工执行计算引擎，与系统输出对比
**发现的差异**：
1. 楼层/总楼层比值不可能（数据映射错误）
2. 噪声等级评级矛盾（同一场景两处结论不一致）
3. 风压计算值偏差（超出合理误差范围）
4. 禁用条款错配：A档文件错误包含B档要求条款

**决议**：建立交叉验证为标准验收步骤（不仅审阅输出，还要反向验证输入）

---

## 验收框架建立 · sprint7

### 决策事件：双角色验收框架定型
**背景**：单一视角审阅导致问题遗漏
**框架**：
- 甲方视角（李Sir）：权力转移效果，18项核查清单
- 架构师视角：规格合规性，计算准确性
**严重级别**：P0（发布阻塞）/ P1（推广前修复）/ P2（迭代优化）

---

## 产品架构确认 · sprint 早期

### 决策事件：产品定性决策
- 问卷：9题生活化问题（非技术语言）→ 已锁定，不扩展
- 输出：四章PDF招标文件（技术参数 / 红线清单 / 验收标准 / 商家评审表）
- 数据链路：reportSnapshot 不可变快照（审计可溯源）
- 平台定位：工具，MVP阶段不做双边平台

### 决策事件：L1/L2商业模式设计
- L1（免费）：参数摘要卡，6项核心参数，即时生成
- L2（付费）：完整四章PDF，价格待种子用户验证后定
- L1设计原则：制造认知缺口，让用户知道"我需要什么"，但不知道"具体标准是否达标"

### 决策事件：前端术语锁定
- 「门窗需求定制」（非「问卷」）
- 「招标文件」（非「报告」）
- 理由：术语本身传递产品定位，影响用户心理预期

---

## 待决策事项（未关闭）

| 事项 | 状态 | 阻塞项 |
|------|------|--------|
| L2定价 | 待定 | 需种子用户验证结果 |
| A档900上限确认 | 待定 | 需承包商价格数据 |
| 内容→产品转化衡量机制 | 未建立 | 小红书引流后跨平台损耗 |
| 审核带宽规模化方案 | 未讨论 | 建议推广前有初步方案 |
