# 门窗招标系统 · PLAYBOOK（认知资产库）

> 可复用的方法论、决策模式和踩坑经验。按主题组织，跨项目通用。

---

## 一、调试方法论

### 1.1 硬编码测试法
**场景：** 数据从A传到B，B说没收到，但A确认发了。
**方法：** 在A处用硬编码值替换真实数据（如 `{ city: '测试城市' }`），观察B是否收到。
- 收到 → 问题在数据本身（格式、序列化、大小）
- 没收到 → 问题在传输通道（有另一条路径在干扰）

**案例：** v3.9.1 数据链路修复。前端 payload 21个键完整，云函数收到空对象。硬编码3个字段后云函数收到了，锁定问题不在通道而在调用源（双重调用）。

### 1.2 全局搜索排除法
**场景：** 怀疑某个函数被多处调用，不确定实际执行路径。
**方法：** `Ctrl+Shift+F` 全局搜索函数名，列出所有调用点，逐一确认。
**关键：** 搜索时用核心函数名（如 `generateReport`），不要搜包装函数名。

**案例：** 搜索 `generateReport` 发现 survey.js、generate-loading.js、result-summary.js 三处调用，才发现双重调用根因。

### 1.3 云函数日志对比法
**场景：** 前端看起来正常，但结果不对。
**方法：** 对比云函数日志的 RequestId 数量和时间戳，判断是否有多次调用。
**关键：** 同一秒出现两条不同 RequestId = 双重调用。

---

## 二、架构决策模式

### 2.1 前端 vs 云函数职责划分
**原则：** 云函数负责纯计算和数据持久化，前端负责业务展示逻辑。
**判断标准：**
- 需要数据库/文件存储 → 云函数
- 需要 CPU 密集计算 → 云函数
- 涉及用户体验文案/展示规则 → 前端
- 需要快速迭代调整 → 前端

**案例：** riskLevel/riskReason/factor_to_action 放在前端（generate-loading.js），因为是展示逻辑，迭代频率高。calculateAll() 放在云函数，因为是核心计算。

### 2.2 数据传参方式选择
| 方式 | 适用场景 | 限制 |
|------|----------|------|
| URL query | 轻量标记（source=survey） | ≤1024字符 |
| wx.setStorageSync | 跨页面传大对象 | 单key ≤1MB |
| 全局变量 getApp() | 生命周期内共享 | 页面刷新丢失 |
| 云函数 event | 前端→后端传参 | 需注意嵌套结构匹配 |

**铁律：** 超过100字符的数据一律用 storage，永远不塞 URL。

### 2.3 接口扩展 vs 接口修改
- **扩展（加字段）：** 安全，向后兼容，不破坏测试 → 放心做
- **修改（改字段名/删字段）：** 危险，可能破坏下游 → 必须全链路排查
- **判断标准：** 现有测试是否会因此失败？不会 = 扩展，会 = 修改

---

## 三、微信小程序踩坑清单

### 3.1 URL 长度限制
**坑：** wx.navigateTo 的 URL 总长度约 1024 字符，超出部分静默截断，不报错。
**表现：** 页面能打开但数据为空，`options.data` 为 `undefined`。
**解法：** 大对象用 storage 传递。

### 3.2 Storage key 不一致
**坑：** 写入用 key A，读取用 key B，两边都不报错，只是读到空。
**表现：** 数据"消失"了，但代码逻辑看起来没问题。
**解法：** 定义常量统一管理 key 名称，或至少全局搜索确认读写配对。

### 3.3 云函数部署遗忘
**坑：** 本地改了云函数代码但忘记"上传并部署"，云端跑的还是旧版本。
**表现：** 本地测试逻辑对但线上结果不对。
**解法：** 每次改云函数后立即部署，部署后看云开发控制台确认时间戳。

### 3.4 双重调用静默覆盖
**坑：** 两处代码同时调用同一个云函数，后返回的结果覆盖先返回的。
**表现：** 结果随机——有时对有时错，取决于哪个调用先返回。
**解法：** 新增调用路径前全局搜索函数名，确认唯一。

---

## 四、行业知识（门窗领域）

### 4.1 五维参数含义
| 参数 | 含义 | 单位 | 方向 | 标准 |
|------|------|------|------|------|
| K值 | 传热系数（保温性能） | W/(m²·K) | 越低越好 | GB/T 8484 |
| Rw | 隔声量 | dB | 越高越好 | GB/T 8485 |
| SHGC | 太阳得热系数（隔热性能） | 无量纲 | 越低越好（热地区） | GB/T 2680 |
| 气密等级 | 气密性分级 | 级 | 越高越好 | GB/T 7106 |
| 安全 | 安全玻璃要求 | 分类 | 按场景强制 | GB 15763 |

### 4.2 预算档位定义
- A档：600-900 元/㎡（经济实用）
- B档：900-1400 元/㎡（品质均衡）
- C档：1400-2000 元/㎡（高性能）
- D档：2000+ 元/㎡（极致性能）

### 4.3 风险等级推导
- **高风险：** 安全需求（老人/儿童/落地窗）与低预算冲突 → 必须升档或接受安全妥协
- **中风险：** 性能需求与预算有张力 → 建议调整优先级
- **低风险：** 需求与预算匹配 → 直接执行

---

## 五、商业决策模式

### 5.1 展示文案放前端的原因
factor_to_action 映射表放前端而非云函数：
- 文案调整频率高，前端改完即生效
- 不需要重新部署云函数
- 体现"顾问价值"而非"机器输出"

### 5.2 免费期策略
限时免费不是"免费试用"，而是"产品验证工具"：
- 免费期收集用户行为数据（完成率、分享率）
- 验证"参数准确性"是否是用户付费动机
- 3个月后有数据支撑再决定定价策略

### 5.3 确定性交付 = 核心卖点
用户买的不是PDF，而是"确定性"：
- 参数有国标依据 → 确定
- 商家必须按标准报价 → 确定
- 对比有统一尺度 → 确定

---

## 六、协作系统设计

### 6.1 文档分层设计
**应用场景：** AI协作项目需要不同层次的信息持久化。
**分层方案：**
- 永久层（CLAUDE.md ≤200行）：协作规范 + 代码红线 + 数据链路
- 累积层（CHANGELOG.md）：版本历史 + 改动记录 + 经验教训
- 资产库（PLAYBOOK.md）：可复用方法论 + 决策模式 + 行业知识

**更新职责：** Claude 在每次会话结束时主动输出更新指令，用户确认执行。

### 6.2 上下文恢复架构
**问题：** AI 对话框无持久记忆，每次新对话从零开始。
**解法：** 三层外部记忆 + AI内置记忆 + 轻量状态块
```
持久记忆（不变的）        动态状态（每次变的）
├─ AI内置记忆             ├─ 8行状态块（用户粘贴）
│  （身份/协作模式/偏好）  │  （commit/待办/阻塞点）
├─ CLAUDE.md              └─ 当次对话上下文
│  （Claude Code自动读取）
├─ CHANGELOG.md
└─ PLAYBOOK.md
```

**效果：** 新对话恢复时间从上传文档+30秒降至粘贴状态块+5秒。

### 6.3 认知复利公式
**公式：** 用户主动反馈 + AI模式识别 + 三层文档沉淀 = 认知复利

**三个要素缺一不可：**
- 用户不反馈 → AI 不知道偏差在哪
- AI 不提炼 → 经验停留在聊天记录，无法复用
- 不写文档 → 下次从零开始，无法累积

**关键行为：** 用户说"这样不好"时，不是抱怨，是在训练系统。每一次校准都通过记忆和文档固化，下次自动生效。

### 6.4 主动校准机制
**场景：** 用户提出方案时，AI发现存在风险或有更优解。
**正确做法：** 立即提出替代方案 + 对比分析，不等用户问。
**错误做法：** 默认同意执行，事后才暴露问题。

**案例：**
- 用户问能否装 Claude HUD → 查证后发现不支持 Windows → 推荐替代方案
- 用户要求对话框生成代码手工粘贴 → 推回建议 Claude Code 自动执行
- 用户每次上传800行文档恢复上下文 → 提出三层瘦身架构替代方案
