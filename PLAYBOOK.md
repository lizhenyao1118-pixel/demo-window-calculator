# 门窗招标系统 · PLAYBOOK（认知资产库）

> 可复用的方法论、决策模式和踩坑经验。按主题组织，跨项目通用。

---

## 一、调试方法论

### 1.1 硬编码测试法
**场景：** 数据从A传到B，B说没收到，但A确认发了。
**方法：** 在A处用硬编码值替换真实数据（如 `{ city: '测试城市' }`），观察B是否收到。
- 收到 → 问题在数据本身（格式、序列化、大小）
- 没收到 → 问题在传输通道（有另一条路径在干扰）

**案例：** v3.9.1 数据链路修复。前端 payload 21个键完整，云函数收到空对象。硬编码3个字段后云函数收到了，锁定问题不在通道而在调用源（双重调用）。

### 1.2 全局搜索排除法
**场景：** 怀疑某个函数被多处调用，不确定实际执行路径。
**方法：** `Ctrl+Shift+F` 全局搜索函数名，列出所有调用点，逐一确认。
**关键：** 搜索时用核心函数名（如 `generateReport`），不要搜包装函数名。

**案例：** 搜索 `generateReport` 发现 survey.js、generate-loading.js、result-summary.js 三处调用，才发现双重调用根因。

### 1.3 云函数日志对比法
**场景：** 前端看起来正常，但结果不对。
**方法：** 对比云函数日志的 RequestId 数量和时间戳，判断是否有多次调用。
**关键：** 同一秒出现两条不同 RequestId = 双重调用。

---

## 二、架构决策模式

### 2.1 前端 vs 云函数职责划分
**原则：** 云函数负责纯计算和数据持久化，前端负责业务展示逻辑。
**判断标准：**
- 需要数据库/文件存储 → 云函数
- 需要 CPU 密集计算 → 云函数
- 涉及用户体验文案/展示规则 → 前端
- 需要快速迭代调整 → 前端

**案例：** riskLevel/riskReason/factor_to_action 放在前端（generate-loading.js），因为是展示逻辑，迭代频率高。calculateAll() 放在云函数，因为是核心计算。

### 2.2 数据传参方式选择
| 方式 | 适用场景 | 限制 |
|------|----------|------|
| URL query | 轻量标记（source=survey） | ≤1024字符 |
| wx.setStorageSync | 跨页面传大对象 | 单key ≤1MB |
| 全局变量 getApp() | 生命周期内共享 | 页面刷新丢失 |
| 云函数 event | 前端→后端传参 | 需注意嵌套结构匹配 |

**铁律：** 超过100字符的数据一律用 storage，永远不塞 URL。

### 2.3 接口扩展 vs 接口修改
- **扩展（加字段）：** 安全，向后兼容，不破坏测试 → 放心做
- **修改（改字段名/删字段）：** 危险，可能破坏下游 → 必须全链路排查
- **判断标准：** 现有测试是否会因此失败？不会 = 扩展，会 = 修改

---

## 三、微信小程序踩坑清单

### 3.1 URL 长度限制
**坑：** wx.navigateTo 的 URL 总长度约 1024 字符，超出部分静默截断，不报错。
**表现：** 页面能打开但数据为空，`options.data` 为 `undefined`。
**解法：** 大对象用 storage 传递。

### 3.2 Storage key 不一致
**坑：** 写入用 key A，读取用 key B，两边都不报错，只是读到空。
**表现：** 数据"消失"了，但代码逻辑看起来没问题。
**解法：** 定义常量统一管理 key 名称，或至少全局搜索确认读写配对。

### 3.3 云函数部署遗忘
**坑：** 本地改了云函数代码但忘记"上传并部署"，云端跑的还是旧版本。
**表现：** 本地测试逻辑对但线上结果不对。
**解法：** 每次改云函数后立即部署，部署后看云开发控制台确认时间戳。

### 3.4 双重调用静默覆盖
**坑：** 两处代码同时调用同一个云函数，后返回的结果覆盖先返回的。
**表现：** 结果随机——有时对有时错，取决于哪个调用先返回。
**解法：** 新增调用路径前全局搜索函数名，确认唯一。

### 3.5 云函数跨目录依赖
**坑：** 云函数部署后每个函数是独立沙箱，无法 `require('../其他云函数/')` 跨函数引用。
**表现：** 本地运行正常，部署后报 "Cannot find module" 或函数不生效。
**解法：** 在目标云函数内建 `shared/` 目录，复制所有依赖文件，修改 require 路径为相对路径。
**关键：** 被复制文件可能有二级依赖（如 shared/calculator-v2.js 依赖 PDF库），需递归检查后一次性复制完整，否则部署后仍报找不到模块。

**案例：** v3.9.2 中 createTender 云函数需要 calculator-v2.js 和 PDF库。最初尝试 `require('../generateReport/shared/calculator-v2')` 失败，改为复制 shared/ 全目录（9个文件）到 createTender，同时修改所有 require 为相对路径 `require('./shared/calculator-v2')`，部署后生效。

---

## 四、行业知识（门窗领域）

### 4.1 五维参数含义
| 参数 | 含义 | 单位 | 方向 | 标准 |
|------|------|------|------|------|
| K值 | 传热系数（保温性能） | W/(m²·K) | 越低越好 | GB/T 8484 |
| Rw | 隔声量 | dB | 越高越好 | GB/T 8485 |
| SHGC | 太阳得热系数（隔热性能） | 无量纲 | 越低越好（热地区） | GB/T 2680 |
| 气密等级 | 气密性分级 | 级 | 越高越好 | GB/T 7106 |
| 安全 | 安全玻璃要求 | 分类 | 按场景强制 | GB 15763 |

### 4.2 预算档位定义
- A档：600-900 元/㎡（经济实用）
- B档：900-1400 元/㎡（品质均衡）
- C档：1400-2000 元/㎡（高性能）
- D档：2000+ 元/㎡（极致性能）

### 4.3 风险等级推导
- **高风险：** 安全需求（老人/儿童/落地窗）与低预算冲突 → 必须升档或接受安全妥协
- **中风险：** 性能需求与预算有张力 → 建议调整优先级
- **低风险：** 需求与预算匹配 → 直接执行

---

## 五、商业决策模式

### 5.1 展示文案放前端的原因
factor_to_action 映射表放前端而非云函数：
- 文案调整频率高，前端改完即生效
- 不需要重新部署云函数
- 体现"顾问价值"而非"机器输出"

### 5.2 免费期策略
限时免费不是"免费试用"，而是"产品验证工具"：
- 免费期收集用户行为数据（完成率、分享率）
- 验证"参数准确性"是否是用户付费动机
- 3个月后有数据支撑再决定定价策略

### 5.3 确定性交付 = 核心卖点
用户买的不是PDF，而是"确定性"：
- 参数有国标依据 → 确定
- 商家必须按标准报价 → 确定
- 对比有统一尺度 → 确定

---

## 六、协作系统设计

### 6.1 文档分层设计
**应用场景：** AI协作项目需要不同层次的信息持久化。
**分层方案：**
- 永久层（CLAUDE.md ≤200行）：协作规范 + 代码红线 + 数据链路
- 累积层（CHANGELOG.md）：版本历史 + 改动记录 + 经验教训
- 资产库（PLAYBOOK.md）：可复用方法论 + 决策模式 + 行业知识

**更新职责：** Claude 在每次会话结束时主动输出更新指令，用户确认执行。

### 6.2 上下文恢复架构
**问题：** AI 对话框无持久记忆，每次新对话从零开始。
**解法：** 三层外部记忆 + AI内置记忆 + 轻量状态块
```
持久记忆（不变的）        动态状态（每次变的）
├─ AI内置记忆             ├─ 8行状态块（用户粘贴）
│  （身份/协作模式/偏好）  │  （commit/待办/阻塞点）
├─ CLAUDE.md              └─ 当次对话上下文
│  （Claude Code自动读取）
├─ CHANGELOG.md
└─ PLAYBOOK.md
```

**效果：** 新对话恢复时间从上传文档+30秒降至粘贴状态块+5秒。

### 6.3 认知复利公式
**公式：** 用户主动反馈 + AI模式识别 + 三层文档沉淀 = 认知复利

**三个要素缺一不可：**
- 用户不反馈 → AI 不知道偏差在哪
- AI 不提炼 → 经验停留在聊天记录，无法复用
- 不写文档 → 下次从零开始，无法累积

**关键行为：** 用户说"这样不好"时，不是抱怨，是在训练系统。每一次校准都通过记忆和文档固化，下次自动生效。

### 6.4 主动校准机制
**场景：** 用户提出方案时，AI发现存在风险或有更优解。
**正确做法：** 立即提出替代方案 + 对比分析，不等用户问。
**错误做法：** 默认同意执行，事后才暴露问题。

**案例：**
- 用户问能否装 Claude HUD → 查证后发现不支持 Windows → 推荐替代方案
- 用户要求对话框生成代码手工粘贴 → 推回建议 Claude Code 自动执行
- 用户每次上传800行文档恢复上下文 → 提出三层瘦身架构替代方案

---

## 七、内容生产工作流 v1.0

### 7.1 标准流程
1. NotebookLM生成初稿
2. Perplexity初审（数据/价格/市场描述）
3. 李sir专业判断+经验注入+定稿
4. Claude对话生成配图（Python+cairosvg）
5. 小红书发布

### 7.2 Perplexity初审员提问模板
凡AI给出具体数字，用以下指令初审：
```
请扮演初审员，标出草稿中所有具体数字/价格/市场描述，
逐条搜索对比，给出✅基本准确/⚠️存疑/❌明显失真标注。
不需要判断工艺对错，不需要解读国标，只做信息核查。
```

### 7.3 配图生成规范
**尺寸规范：**
- 封面：1080×1080，深蓝#1B3A6B背景+橙色#F5820A强调
- 内容图：1080×1440，白/深蓝背景交替
- 字体：Noto Sans CJK SC（服务器端）/ 思源黑体（Canva）
- 每张底部固定署名条：「不卖窗的李sir · 独立门窗顾问」

**生成工具链：**
- Python脚本 + cairosvg 渲染
- 脚本路径：`/home/claude/gen_images.py`（下次可复用）
- 输出格式：PNG

### 7.4 内容排期原则（冷启动期）
**阵地选择：** 唯一阵地小红书图文
**发布节奏：** 每周3篇（周一/周三/周六 20:00）
**内容策略：**
- 前3篇不提小程序，聚焦教育+信任
- 第4篇起引转化（软链接）
- 内容比例：流量4:信任6:转化2（第1个月）

### 7.5 种子用户验证标准
**Pass Criteria：**
- H1（工具完成度）：完成率≥70%
- H2（价值感知）：感知价值≥60%
- H3（商业转化）：付费意愿≥40%

**第01篇执行清单：**
- ✅ 正文定稿
- ✅ 国标数据核实
- ✅ 5张配图生成（1080×1080封面+1080×1440内容图×4）

---

## 八、IP定位与价值主张 · 终稿 [2026-04-01]

### 8.1 生态位
在家装产业链中，李Sir是唯一"懂技术且不卖货"的角色，填补装修公司、门窗商家与业主之间的权力真空。

两个身份同时成立：**技术裁判**（确保国标执行）+ **风险管家**（管理高沉没成本项目的决策风险）。

独立性是护城河。不卖货，所以才值得听。

### 8.2 战略定位
内容战略的核心不是"做门窗科普"，而是"做选窗决策的基础设施"。

| 维度 | 门窗科普型 | 李Sir（决策指南型） |
|------|-----------|------------------|
| 内容终点 | 用户变聪明了 | 用户拿到可执行方案 |
| 用户关系 | 学完即走 | 方案伴随选购全流程 |
| 竞争壁垒 | 低，可复制 | 高，依赖国标数据库+算法 |
| 盈利模式 | 广告/带货（损害独立性） | 工具服务费（强化独立性） |
| 用户粘性 | 弱 | 强（报告持续使用） |

### 8.3 目标用户画像
大多数业主在选购门窗时经历了一个身份错位——明明是花钱的甲方，却把自己逼成了"被迫考证的门窗学徒"。研究得越多，越是在商家的剧本里打转。根本原因是没有掌握"标准"，知识再多也是防御式学习。

### 8.4 核心价值主张
> 别再逼自己当门窗专家了。你需要的不是更多知识，而是：一份清晰的标准、一套好用的工具、一个靠谱的帮手。

### 8.5 产品核心价值
> 通过专业科学的标准体系，在一个不确定的系统工程中，交付**决策的确定性**。

（李Sir交付的是标准+工具+话语权，不是最终结果。）

### 8.6 协作原则（李Sir → Claude）
- 主动挑战假设，质疑推理，有问题直说
- 检查逻辑漏洞、自我安慰、低估的风险
- 建议必须有推理、有依据、有可执行步骤
- 优先让李Sir"成长"，而不是当下舒服
- 坚持合理判断，实话实说，毫无保留

---

## 九、内容生产工作流 v1.1 [2026-04-01]

### 9.1 工作流（更新版）
0. 选题确定
1. NotebookLM 生成初稿
2. Perplexity 数据初审（标出具体数字/价格，核查准确性）
3. 李sir 专业判断 + 经验注入 + 定稿
4. Claude 生成配图（Python+cairosvg）
5. 小红书发布

## 选题框架 v2.0 [2026-04-01]
（替代此前所有选题框架版本）

### 与IP定位的关系
IP定位决定选题边界——"交付决策确定性"而非"传授知识"。
所有选题必须指向"用户拿到可执行方案"，而非"用户变聪明了"。

### 第一关：入场条件（筛选器，是/否题，任一不符直接淘汰）
- 独立第三方视角：能否从"不卖货"的立场给出答案
- 有国标/标准依据：结论可用标准支撑，而非纯经验判断
- 交付可执行方案：用户看完能做一件具体的事
- 赋能而非贩卖焦虑：让用户感到"我能做到"，而非"我快被坑了"

### 第二关：选题价值评分
选题价值 = 痛点强度 × 痛点可见性 × 解决方案稀缺性

**痛点强度**：问题对用户有多痛？直接影响金钱/决策=高；用户觉得无所谓=低
**痛点可见性**：用户知不知道自己有这个问题？主动在搜=高；需要先破认知=低
**解决方案稀缺性**：市面上有没有人能用李Sir角度回答？需要独立视角+国标=高；随便搜到=低

三项均≥中才进入下一关。

### 第三关：痛点解构层次
用户痛点必须拆解为可执行的"任务场景"，并判断层次：

| 层次 | 描述 | 内容效果 |
|------|------|---------|
| L1 | 回应已知痛点：用户知道自己有问题，在主动找答案 | 收藏率高 |
| L2 | 重构认知框架：用户不知道问题可以被重新定义 | 关注率+传播率高 |

李Sir核心竞争壁垒在L2：
"原来我不需要逼自己成为门窗专家，也可以选对窗。"
→ 把"我需要学更多"替换成"我需要一份标准和一个靠谱的帮手"

实操原则：每篇至少一个L1锚点（确保搜索可达）+ 尽量植入一个L2认知颠覆点（建立差异化）。

### 选题评估完整流程
① 入场条件检查（4项，任一不符→淘汰）
② 三维评分：痛点强度 × 痛点可见性 × 解决方案稀缺性（三项均≥中）
③ 痛点解构：确认任务场景 + 判断L1/L2层次
④ 入选，进入内容生产工作流

---

## 十、内容运营基础设施 [2026-04-01]

### 10.1 飞书多维表格（已配置）
文件名：不卖窗的李sir · 内容运营系统
- 选题排期表：看板视图，按状态分组（💡灵感/✅入选/✏️创作中/🔍待审核/📢已发布）
- 内容反馈记录：收藏率公式字段，24h/72h双节点追踪
- Excel备份：content/内容运营系统.xlsx（归档用，不再主动维护）

### 10.2 飞书MCP接入预案（第二阶段，内容节奏稳定后执行）
触发条件：完成6篇发布 + 内容节奏稳定（连续3周不断更）

接入目标：
- Claude Code直接读写飞书多维表格（选题状态更新、数据录入）
- 手机飞书发消息 → 触发选题记录 / 内容任务
- 数据反馈自动写入，减少手动维护摩擦

接入方案参考：
- 文档侧：feishu-mcp（npx @larksuiteoapi/lark-mcp）
- 消息侧：feishu-claudecode Bot（WebSocket长连接）
- 配置入口：飞书开放平台 open.feishu.cn → 企业自建应用

注意事项：
- 配置需要飞书企业自建应用 + App_ID + App_Secret
- Claude Code MCP配置必须用 --scope=user
- 现阶段不接入，避免配置成本干扰内容节奏

---

## 混合执行模型·模型切换操作 [2026-04-01]

工具：ccm（claude-code-switch）

**切换命令：**
- 低风险任务（日常代码/格式修改）：`ccm glm china` → 使用GLM-4.7
- 高风险任务（架构/PDF/核心逻辑）：`ccm claude` → 使用Claude Pro
- 查看当前模型：`ccm status`
- 更新模型映射：`ccm update-config`

**配置文件：** `~/.ccm_config`  
**GLM节点：** 国内用 china，海外用 global

**登录问题应急处理：**
1. 先查 status.claude.com 确认是否服务端故障
2. 终端执行：`claude auth logout` → `claude auth login`
3. 浏览器未自动打开时按 c 复制OAuth URL手动粘贴
